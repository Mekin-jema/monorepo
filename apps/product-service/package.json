{
  "name": "product-service",
  "version": "1.0.0",
  "type": "module",
  "description": "Microservice responsible for managing product catalog data (CRUD operations, categories, inventory sync, and Kafka-based event publishing/consumption) within the e-commerce platform.",
  "main": "src/index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "pnpm dlx tsx --watch src/index.ts --port 3003",
    "check-types": "tsc --noEmit"
  },
  "keywords": [
    "ecommerce",
    "product",
    "catalog",
    "microservice",
    "kafka",
    "inventory"
  ],
  "author": "Mekin Jemal",
  "license": "ISC",
  "dependencies": {
    "@repo/typescript-config": "workspace:*",
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.3",
    "express": "^5.1.0"
  }
}
