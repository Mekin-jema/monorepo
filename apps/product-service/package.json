{
  "name": "product-service",
  "version": "1.0.0",
  "type": "module",
  "description": "Microservice responsible for managing product catalog data (CRUD operations, categories, inventory sync, and Kafka-based event publishing/consumption) within the e-commerce platform.",
  "main": "src/index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "pnpm dlx tsx --watch  src/index.ts --port 3002",
    "check-types": "tsc --noEmit"
  },
  "keywords": [
    "ecommerce",
    "product",
    "catalog",
    "microservice",
    "kafka",
    "inventory"
  ],
  "author": "Mekin Jemal",
  "license": "ISC",
  "dependencies": {
    "@repo/auth": "workspace:*",
    "@repo/db": "workspace:*",
    "@repo/kafka": "workspace:*",
    "better-auth": "^1.3.24",
    "cors": "^2.8.5",
    "express": "^5.0.3"
  },
  "devDependencies": {
    "@repo/types": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.3",
    "dotenv": "^17.2.3"
  }
}
